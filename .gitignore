# Secrets — never commit real credentials
.env
config_local.py

# Runtime state / data (regenerated by the scheduler, not source)
data/
*.db
*.db-journal
scheduler.log
selected_model.json

# Large raw training data (sourced from Kaggle — see README)
*.csv
!docs/model_comparison_results.csv

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# OS / editor
.DS_Store
.vscode/
.idea/
